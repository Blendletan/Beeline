{
  "name": "beeline",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "benchmark": "node scripts/benchmark.ts",
    "build": "tsc -p tsconfig.json",
    "serve": "node scripts/serve.mjs",
    "test": "node --test src/game.test.ts"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
